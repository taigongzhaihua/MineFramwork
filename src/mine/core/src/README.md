# src

此目录用于放置 mine.core 的实现文件。

约定如下：

- 只放置公开头中声明过的非模板实现，避免把可内联模板实现拆到 cpp 中造成链接缺失。
- 每个实现文件应在文件头部提供中文 Doxygen 或说明性注释，解释该文件负责的实现边界。
- 涉及内存、错误传播和字符串缓冲区等关键路径时，应额外说明生命周期、所有权和失败策略。
- 当前实现文件包括：Allocator.cpp、Assert.cpp、String.cpp。