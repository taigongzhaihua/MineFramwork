/**
 * @file Api.h
 * @brief mine.platform.abi 模块 API 导出宏。
 *
 * mine.platform.abi 是纯接口层（头文件库），无实际二进制输出，
 * 因此 MINE_PLATFORM_ABI_API 宏为空占位，用于文档与未来扩展。
 */

#pragma once

/// 平台 ABI 接口导出标记（接口层无需 DLL 导出，此处为空占位）
#define MINE_PLATFORM_ABI_API
